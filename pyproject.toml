[tool.poetry]
name = "alugueis-pandas"
version = "0.1.0"
description = ""
authors = ["Seu Nome <seuemail@exemplo.com>"]

[tool.poetry.dependencies]
python = "^3.12" # Certifique-se que a versão do python esteja correta
pandas = "^2.3.3"
matplotlib = "^3.10.7"

[tool.poetry.group.dev.dependencies]
ipykernel = "^7.1.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"